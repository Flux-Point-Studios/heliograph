//! The `hg-claims` ABI suite (HANDOFF §7): golden vectors, round-trips, the
//! every-byte mutant zoo, fail-closed rejections, and truncation/extension.

mod common;

mod enums;
mod fail_closed;
mod golden;
mod mutation;
mod roundtrip;
mod truncation;
